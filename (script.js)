/**
 * SABRINA AI - MASTER LOGIC CORE (2026) — CORRECTED & IMPROVED
 * Connects to Google's Gemini API with proper error handling,
 * retries, timeouts, streaming support, and best practices.
 */

const SABRINA_CONFIG = {
  // ⚠️ SECURITY: NEVER hardcode the API key!
  // Use environment variable in production:
  // Node.js → process.env.GEMINI_API_KEY
  // Vite/React → import.meta.env.VITE_GEMINI_API_KEY
  apiKey: "AIzaSyDMsEWb684y0FzptAJ8vT5HMxlahV2hWNg", // ← Replace with env var

  // Recommended model (gemini-1.5-flash still works, but consider updating)
  model: "gemini-1.5-flash",

  // Base endpoints
  baseUrl: "https://generativelanguage.googleapis.com/v1beta",

  // System identity — kept separate from user messages
  systemInstruction: "Your name is Sabrina. You are a brilliant, warm, and helpful AI assistant. Be concise, clear, and friendly. Use plain text only — no markdown formatting.",

  maxRetries: 3,
  timeoutMs: 15000, // 15 seconds
};

/** Helper: sleep for ms milliseconds */
const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

/**
 * Core query function with retry logic and timeout.
 * @param {string} userPrompt - The user's message
 * @param {Array} history - Optional conversation history [{role, text}]
 * @returns {Promise<string>} - Sabrina's reply
 */
async function executeQuery(userPrompt, history = []) {
  if (!userPrompt || typeof userPrompt !== "string" || userPrompt.trim() === "") {
    return "Error: No valid input provided.";
  }

  // Build conversation contents (Gemini expects "user" and "model" roles only)
  const contents = [
    ...history.map((msg) => ({
      role: msg.role === "user" ? "user" : "model",
      parts: [{ text: msg.text }],
    })),
    { role: "user", parts: [{ text: userPrompt }] },
  ];

  const body = JSON.stringify({
    systemInstruction: {                    // ← Corrected: camelCase (not snake_case)
      parts: [{ text: SABRINA_CONFIG.systemInstruction }],
    },
    contents,
    generationConfig: {
      temperature: 0.8,
      maxOutputTokens: 1024,
    },
  });

  for (let attempt = 1; attempt <= SABRINA_CONFIG.maxRetries; attempt++) {
    const controller = new AbortController();
    const timer = setTimeout(() => controller.abort(), SABRINA_CONFIG.timeoutMs);

    try {
      const response = await fetch(
        `\( {SABRINA_CONFIG.baseUrl}/models/ \){SABRINA_CONFIG.model}:generateContent?key=${SABRINA_CONFIG.apiKey}`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body,
          signal: controller.signal,
        }
      );

      clearTimeout(timer);

      if (!response.ok) {
        const errData = await response.json().catch(() => ({}));
        const msg = errData?.error?.message || `HTTP ${response.status}`;

        // Do not retry on client errors (bad request, auth, etc.)
        if ([400, 401, 403, 404].includes(response.status)) {
          return `SABRINA_ERROR: ${msg}`;
        }

        // Retry on rate limits or server errors
        if (attempt < SABRINA_CONFIG.maxRetries) {
          await sleep(attempt * 1000); // simple exponential backoff
          continue;
        }
        return `SABRINA_ERROR: ${msg}`;
      }

      const data = await response.json();
      const candidate = data?.candidates?.[0];

      if (candidate?.content?.parts?.[0]?.text) {
        return candidate.content.parts[0].text.trim();
      }

      if (candidate?.finishReason === "SAFETY") {
        return "Sabrina: That topic was flagged by safety filters. Please try rephrasing.";
      }

      return "SABRINA_ERROR: Unexpected response format from API.";

    } catch (error) {
      clearTimeout(timer);

      if (error.name === "AbortError") {
        if (attempt < SABRINA_CONFIG.maxRetries) continue;
        return "CONNECTION_FAILURE: Request timed out. Check your internet connection.";
      }

      if (attempt < SABRINA_CONFIG.maxRetries) {
        await sleep(attempt * 1000);
        continue;
      }

      return `CONNECTION_FAILURE: ${error.message || "Link to API disrupted."}`;
    }
  }
}

/**
 * Streaming version — calls onChunk(text) as each piece arrives.
 * @param {string} userPrompt
 * @param {Array} history
 * @param {function} onChunk - called with each text chunk + full text so far
 * @param {function} onDone - called when streaming is complete
 * @param {function} onError - called on error
 */
async function executeQueryStream(userPrompt, history = [], onChunk, onDone, onError) {
  if (!userPrompt || typeof userPrompt !== "string" || userPrompt.trim() === "") {
    return onError?.("No valid input provided.");
  }

  const contents = [
    ...history.map((msg) => ({
      role: msg.role === "user" ? "user" : "model",
      parts: [{ text: msg.text }],
    })),
    { role: "user", parts: [{ text: userPrompt }] },
  ];

  try {
    const response = await fetch(
      `\( {SABRINA_CONFIG.baseUrl}/models/ \){SABRINA_CONFIG.model}:streamGenerateContent?key=${SABRINA_CONFIG.apiKey}&alt=sse`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          systemInstruction: {                  // ← Corrected: camelCase
            parts: [{ text: SABRINA_CONFIG.systemInstruction }],
          },
          contents,
          generationConfig: {
            temperature: 0.8,
            maxOutputTokens: 1024,
          },
        }),
      }
    );

    if (!response.ok) {
      const errData = await response.json().catch(() => ({}));
      return onError?.(`API Error: ${errData?.error?.message || response.status}`);
    }

    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    let fullText = "";

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;

      const chunk = decoder.decode(value, { stream: true });

      for (const line of chunk.split("\n")) {
        if (!line.startsWith("data: ")) continue;

        const raw = line.slice(6).trim();
        if (!raw || raw === "[DONE]") continue;

        try {
          const parsed = JSON.parse(raw);
          const text = parsed?.candidates?.[0]?.content?.parts?.[0]?.text || "";

          if (text) {
            fullText += text;
            onChunk?.(text, fullText);
          }
        } catch (e) {
          // Ignore malformed JSON lines
        }
      }
    }

    onDone?.(fullText);
  } catch (err) {
    onError?.(err.message || "Streaming connection failed.");
  }
}

// Named exports (uncomment if using ES modules)
export { executeQuery, executeQueryStream, SABRINA_CONFIG };

// Example usage:
// executeQuery("What is the speed of light?").then(console.log);

// executeQueryStream(
//   "Tell me a joke",
//   [],
//   (chunk) => process.stdout.write(chunk),
//   (full) => console.log("\nDone:", full),
//   (err) => console.error("Error:", err)
// );
